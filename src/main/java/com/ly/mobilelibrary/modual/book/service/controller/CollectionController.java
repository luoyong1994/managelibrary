package com.ly.mobilelibrary.modual.book.service.controller;

import com.ly.mobilelibrary.base.model.Collectionor;
import com.ly.mobilelibrary.base.util.JsonResult;
import org.springframework.web.bind.annotation.RestController;

@RestController
public class CollectionController {

    public JsonResult addCollection(Collectionor collectionor){

        return new JsonResult();
    }


    public JsonResult updateCollection(Collectionor collectionor){

        return new JsonResult();
    }

    public JsonResult queryCollection(Collectionor collectionor){

        return  new JsonResult();
    }
}
